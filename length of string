#include <stdio.h>
int main(){

char str [100];
int i,length=0;
printf("Enter the string:");
fgets (str,20,stdin);
for (i=0;str[i]!='\0';i++)
{
length++;
}
printf("\n length of input string is %d",length);
return 0;
}
