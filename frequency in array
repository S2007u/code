// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a[10],i,freq=0,key;
    for(i=0;i<10;i++)
    {
    printf("Enter the number:");
    scanf("%d",&a[i]);
    }
    printf("Enter the number to find:");
    scanf("%d",&key);
    for(i=0;i<10;i++)
    
    if(a[i]==key)
    freq++;
    printf("Frequency of %d is %d",a[i],key);

    return 0;
}
