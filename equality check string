#include <stdio.h>
#include <string.h>
int main()
{
char first_str[] = "SUBHAM";
char second_str[] = "SHUBHAM";

int res = strcmp(first_str, second_str);
if (res == 0)
printf("Strings are equal");
else
printf("Strings are unequal");
 printf("\nValue returned by strcmp() is: %d", res);
 return 0;
}
